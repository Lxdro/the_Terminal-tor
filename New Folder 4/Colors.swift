import SwiftUI

struct TTYColors {
    static let background = Color.black
    static let text = Color(red: 0.0, green: 1.0, blue: 0.0)
    static let red = Color(red: 1.0, green: 0.0, blue: 0.0)
    static let dimText = Color.green.opacity(0.7)
    static let terminalBlack = Color(red: 0.1, green: 0.1, blue: 0.1)
    static let highlight = Color.green.opacity(0.2)
}
